name: docker-build.yml
on:
  push:
    branches:
      - Dockerfile
jobs:
  build:
    name: docker-build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: login-docker
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}
      - name: build-image
        run: docker build -t ${{ secrets.DOCKER_USER }}/devopscourse:team1-${{ github.actor }}-${{ github.run_number }} .
      - name: push-image
        run: docker push ${{ secrets.DOCKER_USER }}/devopscourse:team1-${{ github.actor }}-${{ github.run_number }}